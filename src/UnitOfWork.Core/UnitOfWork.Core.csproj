<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- Cho phép project test truy cập các member `internal` (operation lease, ...) -->
    <InternalsVisibleTo Include="UnitOfWork.Tests" />
  </ItemGroup>

</Project>
